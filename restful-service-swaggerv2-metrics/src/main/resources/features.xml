<?xml version="1.0" encoding="UTF-8"?>
<features name="restful-service-swaggerv2-metrics-${project.version}"
          xmlns="http://karaf.apache.org/xmlns/features/v1.0.0">

    <repository>mvn:org.apache.cxf.karaf/apache-cxf/${cxf.version}/xml/features</repository>
    <repository>mvn:io.fabric8/fabric8-karaf/${fabric8.version}/xml/features</repository>
    <repository>mvn:org.apache.camel.karaf/apache-camel/${camel.version}/xml/features</repository>

    <feature name="restful-service-swaggerv2-metrics" version="${project.version}" resolver="(obr)">
        <feature version="${fabric8.version}">fabric-cxf</feature>
        <feature version="${fabric8.version}">fabric-cxf-registry</feature>
        <feature version="${camel.version}">camel-jackson</feature>
        <feature version="${camel.version}">camel-metrics</feature>
        <bundle>mvn:org.jeannyil.fuse.cxfrs.swaggerv2/restful-service-swaggerv2-metrics/${project.version}</bundle>
    </feature>

</features>